Solr Database
-------------

The `db-solr` tool provides direct access to the Solr databases. Run direct queries against Solr indexing from the `db-solr` tool using standard Solr syntax. 

.. image:: images/db-solr.png

